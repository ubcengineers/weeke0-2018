import styled from 'styled-components';

export const StyledWrapper = styled.div`
    width: 100%;
    height: 50vw;
    max-height: 50vh;
    padding-top: 64px;
    display: flex;
    justify-content: center;
    align-items: center;
`

export const StyledLogo = styled.img`
    width: 60vw;
    background: radial-gradient(#2f2f6f 50%, transparent 100%);
`